# Local development MQTT broker configuration only.
# Do not use anonymous access in shared, staging, or production environments.

listener 1883 0.0.0.0
allow_anonymous true
persistence false

log_dest stdout
