# v0.9.1 Notification Stabilization & Demo Readiness

## Highlights

- Improved the notification service demo script with direct service health, API Gateway health, API Gateway readiness, optional Redpanda publishing, notification listing, and mark-read flow.
- Verified notification sample payloads under `docs/examples/notifications/` for surveillance alert and webhook preference demos.
- Added smoke coverage for direct notification-service health and API Gateway notification proxy health/readiness checks.
- Expanded README demo instructions for publishing surveillance alert events, listing notifications, marking notifications as read, configuring webhook preferences, and checking notification metrics.
- Added troubleshooting guidance for service health, Redpanda publishing, webhook delivery, missing notifications, JWT/RBAC failures, and database migrations.

## Compatibility

- No breaking changes.
- No new services were added.
- No frontend, Kubernetes, or Helm changes were introduced.
